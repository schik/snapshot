{
    "## Comment" = "Do NOT change this file, Gorm maintains it";
    Attributes = {
	Actions = (
	);
	Outlets = (
	    window,
	    mainBox,
	    topBox,
	    iconView,
	    titleField,
	    sizeField,
	    dateField,
	    imageSizeField,
	    timeField,
	    exposureField
	);
	Super = NSObject;
    };
    FirstResponder = {
	Actions = (
	    "closeMainWin:",
	    "computeSize:",
	    "okLoginAction:",
	    "orderFrontFontPanel:",
	    "showInfo:"
	);
	Super = NSObject;
    };
}