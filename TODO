WISHLIST
--------
- Immediately delete downloaded images if user wishes so.
- Add functionality to immediately open an image without downloading it first
  to the local file system (needs of course download to temporary location, but
  you know what I mean).
- Add download via DnD and Copy/Paste.
